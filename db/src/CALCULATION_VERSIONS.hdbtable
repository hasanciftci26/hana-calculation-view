COLUMN TABLE "CALCULATION_VERSIONS" (
    CALCULATION_VERSION_ID INTEGER,
    CALCULATION_ID INTEGER,
    CALCULATION_VERSION_TYPE NVARCHAR(5),
    NAME NVARCHAR(50),
    PRIMARY KEY (CALCULATION_VERSION_ID, CALCULATION_ID)
)